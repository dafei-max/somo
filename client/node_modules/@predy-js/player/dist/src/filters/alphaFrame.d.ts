import type { Composition } from '../player/Composition';
import type { FilterDefine, FilterShaderDefine } from '../render/filter';
export interface AlphaFrameFilter {
    colorRange?: [x: number, y: number];
    alphaRange?: [x: number, y: number];
}
export declare function precompileAlphaFrame(): FilterShaderDefine[];
export declare function registerAlphaFrameFilter(comp: Composition, filter: AlphaFrameFilter): FilterDefine;
