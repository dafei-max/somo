import type { Shader } from '@predy-js/render-interface';
import { Mesh } from '@predy-js/render-interface';
import type { Composition } from '../player/Composition';
export declare function cloneMeshWithShader(mesh: Mesh, shader: Shader): Mesh;
export declare function cloneSpriteMesh(spriteMesh: Mesh, composition: Composition, options: {
    fragment: string;
}): Mesh;
