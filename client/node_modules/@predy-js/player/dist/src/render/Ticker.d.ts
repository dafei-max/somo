interface TickerCallback {
    requestAnimationFrame: typeof window.requestAnimationFrame;
    cancelAnimationFrame: typeof window.cancelAnimationFrame;
}
export declare class Ticker {
    _tickers: ({
        tick: (dt: number) => void;
    } | null)[];
    private _paused;
    private _lastTime;
    private _targetFPS;
    private _interval;
    private _intervalId;
    private _resetTickers;
    private _callback;
    private _startedLoop;
    constructor(fps: number, callback?: TickerCallback);
    get fps(): number;
    set fps(fps: number);
    get paused(): boolean;
    startRunLoop(): void;
    start(): void;
    stop(): void;
    pause(): void;
    resume(): void;
    _tick(): void;
    addTicker(tickObject: {
        tick: (dt: number) => void;
    }): void;
    removeTicker(tickObject: any): void;
    nextTick(callback: (dt: null) => void): {
        tick: (dt: any) => void;
    };
}
export {};
