import type { BezierPathValue, vec3, BezierCurveValue, HermitCurve } from '@predy-js/specification';
import fitCurve from 'fit-curve';
export { fitCurve };
export type FittedBezier3DPoints = Array<[p0: vec3, cp0: vec3, cp1: vec3, p1: vec3]>;
export declare function hermitToBezier(hermit: HermitCurve, steps?: number): BezierCurveValue;
export declare function fitBezierPath(points: vec3[], timestamps: number[], opt?: {
    steps: number;
    error: number;
}): BezierPathValue;
