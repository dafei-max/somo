import type { Composition, PredyPlayerRenderFrame } from '../module';
import { Mesh } from '@predy-js/render-interface';
declare const PredyPlugin: any;
export declare class DrawingFramePlugin extends PredyPlugin {
    _aspect: number;
    composition: Composition;
    _meshToAdd?: Mesh;
    mesh?: Mesh;
    get aspect(): number;
    set aspect(v: number);
    onCompositionReset(composition: any, renderFrame: any): void;
    changeAspect(aspect: any): void;
    prepareRenderFrame(composition: any, frame: PredyPlayerRenderFrame): boolean;
}
export declare function registerDrawingFramePlugin(): void;
export {};
