import type { PlayerScene, PredyPlayer as IPlayer, PredyPlayerConfiguration, PredyPlayerConstructor, PredyPlayerLoadOptions, PredyPlayerPlayOptions, PredyPlayerSnapshotOptions, PredyTouchEventArg } from '../../type';
import { Composition } from './Composition';
import { Ticker } from '../render/Ticker';
import { Renderer } from '@predy-js/render-interface';
import type { vec3 } from '@predy-js/specification';
import type { Canvas } from '@predy-js/render-interface/dist/types/Canvas';
import { EventSystem } from '../event/EventSystem';
import type { mat4 } from '@predy-js/render-interface/types/type';
export declare const helpLink = "https://docs.xiaohongshu.com/doc/ffd9517baa4d190c03d1c1b236aa1054";
export declare const EVENT_TYPE_CLICK = "click";
export declare function disableAllPlayer(disable: boolean): void;
export declare function isCanvasUsedByPlayer(canvas: HTMLCanvasElement): boolean;
export declare function getPlayerByCanvas(canvas: HTMLCanvasElement): PredyPlayer;
export declare function getActivePlayers(): PredyPlayer[];
export declare class PredyPlayer implements IPlayer {
    displayAspect: number;
    readonly container?: HTMLCanvasElement;
    readonly env: string;
    readonly renderer: Renderer;
    private _pixelRatio;
    readonly event: EventSystem;
    readonly canvas: HTMLCanvasElement | Canvas;
    readonly compositions: Composition[];
    readonly ticker: Ticker;
    readonly name: string;
    private readonly _onWebGLContextLost;
    private readonly onMessageItem;
    private readonly _reportGPUTime?;
    private _forceRenderNextFrame;
    private _forwardingTime;
    private _clearPipeline;
    private _resumePending?;
    private _offscreenMode;
    private _ndcMatrix;
    private isDestroyed;
    private _textureOffloaded;
    constructor(options: PredyPlayerConstructor);
    snapshot(options: PredyPlayerSnapshotOptions): void;
    get currentCompositionName(): string;
    get hasPlayable(): boolean;
    get paused(): boolean;
    get interactive(): boolean;
    set interactive(t: boolean);
    get pixelRatio(): number;
    resizeToAspect(aspect: any): void;
    _getTargetSize(parentEle: any): any[];
    resize_web(): void;
    resize_rn(): void;
    resize(): void;
    private updateNDCMatrix;
    getNDCMatrix(): mat4;
    /**
     * 自定义事件
     * @param args
     */
    dispatchCustomEvent(args: Record<string, string>): void;
    _tick(dt: any, forceRender: any): void;
    private _getGPUQuery;
    private _getGPUTime;
    tick(dt: any): void;
    onRenderError(player: any, err: any): void;
    loadSceneAsync(url: any, opt?: PredyPlayerLoadOptions): Promise<PlayerScene>;
    clearCanvas(immediate: any): void;
    config(options: PredyPlayerConfiguration): void;
    initializeComposition(scene: PlayerScene, options?: PredyPlayerPlayOptions): Composition;
    _play(scene: PlayerScene | Composition, options?: PredyPlayerPlayOptions, asyncCallback?: (comp: Composition) => void): Composition;
    play(scene: PlayerScene | Composition, options?: PredyPlayerPlayOptions): Composition;
    playAsync(scene: PlayerScene | Composition, options?: PredyPlayerPlayOptions): Promise<Composition>;
    forwardCompositionTime(composition: Composition, timeInSeconds: number): void;
    _renderFrame(options: any): void;
    _onClick: (e: PredyTouchEventArg) => void;
    _onResume: () => void;
    get2DPointInContainer(composition: Composition, point3D: vec3): number[];
    onItemClicked(e: any): void;
    _offloadTexture(): void;
    pause(options?: {
        offloadTexture?: boolean;
    }): boolean;
    resume(): Promise<void>;
    resumeCallback(): void;
    resumeAsync(): Promise<void>;
    onPlayableUpdate(event: {
        playing: boolean;
        player: PredyPlayer;
    }): void;
    onPausedByItem(event: {
        player: PredyPlayer;
        name: string;
    }): void;
    destroy(keepCanvas?: boolean): void;
    useDowngradeImage(url: any): void;
    destroyItem(id: string, opt: any): void;
}
