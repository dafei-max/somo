import { VFXItem } from '../../player/VFXItem';
import type { TreeOptions } from './TreeItem';
import { Tree } from './TreeItem';
export interface TreeItemOptions {
    tree: TreeOptions;
}
export declare class TreeVFXItem extends VFXItem<Tree> {
    options: TreeItemOptions;
    onUpdateByLoader: (dt: number) => void;
    get type(): "tree";
    onConstructed(options: any): void;
    _createContent(): Tree;
}
