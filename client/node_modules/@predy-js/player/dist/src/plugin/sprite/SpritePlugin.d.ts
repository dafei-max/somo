import type { Composition } from '../../player/Composition';
import type { PredyPlayerRenderFrame } from '../../render/PredyPlayerRenderFrame';
import type { LayerInfo } from './SpriteGroup';
import type { VFXItem } from '../../player/VFXItem';
import type { FilterDefine } from '../../render/filter';
import type { Renderer, Shader } from '@predy-js/render-interface';
import type { PredyPlayerPluginLoadOptions, PredyPluginLoadedScene } from '../../player/PluginSystem';
import { PredyPlugin } from '../../player/PluginSystem';
export declare class SpritePlugin extends PredyPlugin {
    layerInfo?: LayerInfo;
    name: string;
    static precompileAsync(json: any, renderer: Renderer): Promise<any>;
    static prepareResourceAsync(scene: PredyPluginLoadedScene, options: PredyPlayerPluginLoadOptions): Promise<any>;
    spriteMeshShaderFromFilter(filter: FilterDefine, options?: {
        env?: string;
        count?: number;
        ignoreBlend?: boolean;
    }): Shader;
    onCompositionDestroyed(composition: Composition): void;
    onCompositionReset(composition: Composition, pipeline: PredyPlayerRenderFrame): void;
    onCompositionItemLifeBegin(composition: any, item: VFXItem<any>): void;
    onCompositionItemRemoved(composition: Composition, item: VFXItem<any>): void;
    onCompositionDidUpdate(composition: Composition, dt: number): void;
    prepareRenderFrame(composition: Composition, renderFrame: PredyPlayerRenderFrame): boolean;
    postProcessFrame(composition: Composition, pipeline: PredyPlayerRenderFrame): void;
}
