import { SpriteItem } from './SpriteItem';
import { SpriteMesh } from './SpriteMesh';
import { VFXItem } from '../../player/VFXItem';
import type { Composition } from '../../player/Composition';
import type { vec4, vec2, vec3 } from '@predy-js/math';
import type { HitTestTriangleParams } from '../interact/ClickHandler';
export interface SpriteVFXItemRenderData {
    position: vec3;
    size: vec2;
    quat: vec4;
    color: vec4;
}
export declare class SpriteVFXItem extends VFXItem<SpriteItem> {
    _content: SpriteItem;
    composition: Composition;
    cachePrefix: string;
    protected sprite: any;
    get type(): string;
    set type(v: string);
    onConstructed(options: any): void;
    _createContent(renderer: any): SpriteItem;
    onLifetimeBegin(composition: Composition, content: SpriteItem): void;
    onItemRemoved(composition: any, content?: SpriteItem): void;
    _onHideChanged(hide: boolean): void;
    getCurrentRenderData(): SpriteVFXItemRenderData | void;
    getCurrentPosition(): vec3;
    getHitTestParams(force?: any): HitTestTriangleParams | void;
    createWireframeMesh(item: SpriteItem, color: vec4): SpriteMesh;
}
