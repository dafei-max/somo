import type { BaseItemTransform, vec3 } from '@predy-js/specification';
import { Transform } from '../../math/Transform';
export { Transform };
export declare class BaseTransformer {
    readonly base: {
        p: vec3;
        r: vec3;
        s: vec3;
        o: number;
    };
    readonly skip: {
        p?: boolean;
        r?: boolean;
        s?: boolean;
        o?: boolean;
    };
    private readonly posT?;
    private posX?;
    private posY?;
    private posZ?;
    private rotT?;
    private rotX?;
    private rotY?;
    private rotZ?;
    private readonly sclT?;
    private sclX?;
    private sclY?;
    private sclZ?;
    private opT?;
    constructor(options: BaseItemTransform);
    updateLifetime(lifetime: number, transform: Transform): void;
}
