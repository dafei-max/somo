import type { VFXItem } from '../../player/VFXItem';
export declare class Node<T extends {
    parentId?: string;
    id: string;
}> {
    content: T;
    children: Node<T>[];
    constructor(content: T);
    static removeContent<T extends {
        parentId?: string;
        id: string;
    }>(nodes: Node<T>[], content: T): void;
}
export declare class CalculateGroup {
    _nodes: Node<VFXItem>[];
    _itemMap: Record<string, VFXItem>;
    _childrenMap: Record<string, VFXItem[] | undefined>;
    _pendingChildren: Record<string, VFXItem[]>;
    addItems(items: VFXItem[]): void;
    getItemChildren(id: string): VFXItem<void>[];
    pendingItemToParentId(item: VFXItem): void;
    clearPendingChildren(item: VFXItem): void;
    private addItem;
    _removeItemRef(id: any): void;
    removeItemRef(id: any): void;
}
