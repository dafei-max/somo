declare class LinkNode<T> {
    content: T;
    next: LinkNode<T> | null;
    pre: LinkNode<T> | null;
    constructor(content: T);
}
export declare class Link<T> {
    first: LinkNode<T>;
    last: LinkNode<T>;
    length: number;
    _sort: (a: T, b: T) => number;
    constructor(func: (a: T, b: T) => number);
    findNodeByContent(filter: any): LinkNode<T>;
    _insertNode(a: any, next: any): void;
    shiftNode(content: T): LinkNode<T>;
    pushNode(content: T): LinkNode<T>;
    removeNode(node: LinkNode<T>): void;
    forEach(func: (content: T, index: number) => void, thisObj?: any): void;
    forEachReverse(func: (content: T, index: number) => void, thisObj?: any): void;
}
export {};
