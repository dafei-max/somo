import { VFXItem } from '../../player/VFXItem';
import { PredyPlugin } from '../../player/PluginSystem';
import type { CameraOptions } from '@predy-js/specification';
import type { Composition } from '../../player/Composition';
export declare class CameraVFXItem extends VFXItem {
    camera: CameraOptions;
    get type(): "6";
    set type(v: "6");
    onConstructed(options: any): void;
}
export declare class CameraVFXItemLoader extends PredyPlugin {
    order: number;
    currentItems: CameraVFXItem[];
    onCompositionDidUpdate(composition: Composition, dt: number): void;
    onCompositionReset(composition: any, frame: any): void;
    onCompositionItemLifeBegin(composition: any, item: VFXItem<any>): void;
    onCompositionItemRemoved(composition: any, item: VFXItem<any>): void;
}
