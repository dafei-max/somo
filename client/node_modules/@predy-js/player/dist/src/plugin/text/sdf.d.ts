import type { SDFImageOptions, SDFImage, SDFString, FontBase } from '@predy-js/specification';
export declare const fontStyleMap: {
    1: string;
    0: string;
    2: string;
};
export declare function getTextLineSize(lines: SDFString[], fonts: FontBase[]): Float32Array;
export declare function generateSDFImage(options: SDFImageOptions): SDFImage;
export declare class TinySDF {
    cutoff: number;
    draw(imgData: ImageData, padding: number): {
        data: Uint8Array;
        width: number;
        height: number;
        channel: number;
        maxDistance: number;
    };
}
export declare function nearestPowerOfTwo(value: number): number;
