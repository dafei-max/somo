import { VFXItem } from '../../player/VFXItem';
import type { FontBase, TextItem } from '@predy-js/specification';
import type { TextSystem } from './TextSystem';
import type { Composition } from '../../player/Composition';
import type { vec4 } from '@predy-js/render-interface';
export declare class TextVFXItem extends VFXItem {
    textOptions: TextItem['text'];
    system?: TextSystem;
    font: FontBase;
    textDirty: boolean;
    private _text;
    _textLine: string[];
    _textLineIndex: number[];
    private _multiLine;
    readonly meshKey: string;
    _lineSpace: number;
    _bubbleTextureIndex?: number;
    _bubblePadding: vec4;
    _bubbleSlice: [number[], number[]];
    renderer: TextItem['renderer'] & {
        mask: number;
    };
    get type(): "9";
    get text(): string;
    set text(s: string);
    get materialCacheId(): string;
    onConstructed(options: TextItem): void;
    onLifetimeBegin(composition: Composition, content: any): void;
    onItemRemoved(composition: any, content?: any): void;
}
