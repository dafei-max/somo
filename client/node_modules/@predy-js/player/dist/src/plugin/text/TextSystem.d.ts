import type { mat4, Renderer } from '@predy-js/render-interface';
import { Mesh, Texture } from '@predy-js/render-interface';
import type { TextVFXItem } from './TextVFXItem';
import type { SDFImage, TextItem } from '@predy-js/specification';
import type { Composition } from '../../player/Composition';
import type { VFXItem } from '../../player/VFXItem';
declare class TextGeo {
    mesh: Mesh;
    cid: string | undefined;
    items: TextVFXItem[];
    sdfImage: SDFImage | undefined;
    lineNum: number;
    materialId: string;
}
export declare class TextSystem {
    maxPackItem: number;
    items: TextVFXItem[];
    meshesToAdd: Mesh[];
    meshesToRemove: Mesh[];
    meshMap: Record<string, TextGeo>;
    textMap: Record<number, string>;
    itemGroup: TextVFXItem[][];
    groupLineNumMap: Record<number, number>;
    renderer: Renderer;
    composition: Composition;
    env: string;
    indexBuffer: Uint16Array;
    renderingMeshes: string[];
    cacheMat4: mat4;
    textures: Texture[];
    bubbleMeshMap: Record<string, Mesh>;
    cacheSDFImage: Record<string, SDFImage>;
    constructor(comp: Composition);
    generateBubbleMesh(item: TextVFXItem): void;
    destroy(): void;
    onItemBegin(item: VFXItem): void;
    addInvisibleItem(item: VFXItem): void;
    removeInvisibleItem(item: VFXItem): void;
    modifyGroupWithInvisibleItem(index: number, remove: boolean): void;
    onTextItemBegin(item: TextVFXItem): void;
    addItem(item: TextVFXItem): void;
    removeItem(item: TextVFXItem): void;
    updateGroupGeometry(textGeo: TextGeo, texture?: Texture): void;
    /**
       * update meshes data
       */
    updateMeshes(): void;
    attributeDataBySDFImage(sdfImage: SDFImage, lineIndex: number, opt: TextItem['text'], lineFontSize: number, textIndex: number, itemLineOffset: number): Float32Array;
    createGroupTextMesh(item: TextVFXItem, addToMeshes?: boolean): void;
    createTextMesh(item: TextVFXItem, attributeData?: Float32Array): Mesh;
}
export {};
