import { GyroscopeVFXItem } from './GyroscopeVFXItem';
import type { Composition, PredyPluginLoadedScene, VFXItem } from '../../../type';
import { PredyPlugin } from '../../../type';
export declare class GyroscopePlugin extends PredyPlugin {
    order: number;
    private oriManager?;
    currentItems: GyroscopeVFXItem[];
    setOrientationListener(): void;
    onCompositionConstructed(composition: Composition, scene: PredyPluginLoadedScene): void;
    onCompositionItemLifeBegin(composition: Composition, item: VFXItem<any>): void;
    onCompositionDidUpdate(composition: Composition, dt: number): void;
    onCompositionReset(composition: any, frame: any): void;
    onCompositionItemRemoved(composition: any, item: VFXItem<any>): void;
    onCompositionDestroyed(composition: Composition): void;
}
