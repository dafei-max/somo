import type { vec4 } from '@predy-js/specification';
interface StabilizerConfig {
    sensitivity?: number;
    maxWindow?: number;
}
export declare class QuatStabilizer {
    private samples;
    private baseQ;
    private dynamicWindow;
    private noiseLevel;
    private isWalking;
    private shakeSmoothing;
    private currentShake;
    private sensitivity;
    private readonly maxWindow;
    constructor(config?: StabilizerConfig);
    get length(): number;
    get shakeLevel(): number;
    process(q: vec4): vec4 | undefined;
    private analyzeMotion;
    private calculateRawShakeLevel;
    private autoTuneWindow;
    private calcSmartBase;
    private calcWeights;
    private weightedAverage;
    private normalize;
    private angleBetween;
    private slerp;
}
export {};
