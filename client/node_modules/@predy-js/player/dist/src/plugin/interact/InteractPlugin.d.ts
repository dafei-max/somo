import type { PredyPluginLoadedScene } from '../../player/PluginSystem';
import { PredyPlugin } from '../../player/PluginSystem';
import type { PredyPlayerRenderFrame } from '../../render/PredyPlayerRenderFrame';
import type { Composition } from '../../player/Composition';
import type { VFXItem } from '../../player/VFXItem';
import type { InteractMesh } from './InteractMesh';
import type { PredyPlayerPlayOptions } from '../../player/type';
export declare class InteractPlugin extends PredyPlugin {
    private _added;
    private _pass;
    private _messes;
    playOptions?: PredyPlayerPlayOptions;
    addMesh(mesh: InteractMesh): void;
    removeMesh(mesh: InteractMesh, frame: PredyPlayerRenderFrame): void;
    onCompositionConstructed(composition: Composition, scene: PredyPluginLoadedScene): void;
    onCompositionItemLifeBegin(composition: Composition, item: VFXItem<any>): void;
    onCompositionItemRemoved(composition: Composition, item: VFXItem<any>): void;
    prepareRenderFrame(composition: any, pipeline: PredyPlayerRenderFrame): boolean;
}
