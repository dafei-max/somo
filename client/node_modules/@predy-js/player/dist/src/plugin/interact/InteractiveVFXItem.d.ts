import { VFXItem } from '../../player/VFXItem';
import type { HitTestTriangleParams } from './ClickHandler';
import type { Composition } from '../../player/Composition';
import { InteractMesh } from './InteractMesh';
import type { InteractItem } from '@predy-js/specification';
import type { PredyTouchEventArg } from '../../event/type';
import type { EventSystem } from '../../event/EventSystem';
export declare class InteractiveVFXItem extends VFXItem<any> {
    clickable: boolean;
    ui: InteractItem;
    _previewContent: InteractMesh | null;
    _dragEvent: PredyTouchEventArg | null;
    _bouncingArg: PredyTouchEventArg | null;
    get type(): "4";
    set type(v: "4");
    onConstructed(options: any): void;
    _createContent(renderer: Composition): any;
    onLifetimeBegin(renderer: Composition, content: any): void;
    onEnd(): void;
    beginDragTarget(options: any, eventSystem: EventSystem): void;
    _onDragMove(ev: any, e: any): void;
    onItemUpdate(dt: any, lifetime: any): void;
    endDragTarget(): void;
    onItemRemoved(renderer: any, content: any): void;
    getHitTestParams(force?: boolean): HitTestTriangleParams | void;
}
