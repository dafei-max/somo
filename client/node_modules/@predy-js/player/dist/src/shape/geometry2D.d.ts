import type { vec2 } from '@predy-js/render-interface';
type Point2d = vec2;
type BezierPoint = [x: number, y: number, inx: number, iny: number, outx: number, outy: number];
type Geometry = {
    s: number[][];
    p: BezierPoint[];
    c?: Point2d;
    t: number;
};
export type Shape2D = {
    gs?: Geometry[];
    t: number;
    g: Geometry;
};
export declare function getGeometry2DTriangles(geometry: Geometry, options: {
    indexBase?: number;
    uvTransform?: number[];
}): {
    aPoint: Float32Array;
    index: Uint16Array;
};
export {};
