import { ValueType } from '@predy-js/specification';
export type KeyFrame = [inTangent: number, outTangent: number, time: number, value: number];
export declare class ValueGetter<T> {
    constructor(arg: any);
    onCreate(props: any): void;
    getIntegrateValue(t0: number, t1: number, timeScale?: number): T;
    getIntegrateByTime(t0: any, time: any, ts?: number): T;
    getValue(time?: number): T;
    toUniform(meta: KeyFrameMeta): Float32Array;
    map(func: (n: T) => T): void;
    scaleXCoord(scale: any): ValueGetter<T>;
}
export interface KeyFrameMeta {
    curves: ValueGetter<any>[];
    index: number;
    max: number;
    lineSegCount: number;
    curveCount: number;
}
export declare function getKeyFrameMetaByRawValue(value: [type: ValueType, value: any], meta: KeyFrameMeta): void;
export declare function createKeyFrameMeta(): KeyFrameMeta;
