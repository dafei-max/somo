import { ValueGetter } from './ValueGetterBase';
import type { vec3, BezierPathValue, BezierCurveValue } from '@predy-js/specification';
import { HermitCurveValue } from './HermitValueGetter';
export declare class BezierPathValueGetter extends ValueGetter<vec3> {
    _data: BezierPathValue;
    private _options;
    private _out;
    onCreate(props: BezierPathValue): void;
    getValue(time?: number): vec3;
}
export declare class BezierValueGetter extends HermitCurveValue {
    protected _data: BezierCurveValue;
    protected _options: BezierResult;
    onCreate(props: BezierCurveValue): void;
    mapTime(t: number): number;
    getValue(time?: number): number;
    _integrate(time: number, byTime: boolean): number;
}
interface BezierResult {
    range?: number[];
    segIndex: number;
    y: number;
}
export {};
