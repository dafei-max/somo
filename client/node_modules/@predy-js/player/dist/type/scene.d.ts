import type { BaseItemTransform, ItemType } from '@predy-js/specification';
interface CompositionGraphics {
    name: string;
    id: string;
    nodes: CompositionGraphicsNode[];
}
interface CompositionGraphicsNode {
    name: string;
    id: string;
    type: ItemType | 'model' | 'tree';
    transform: BaseItemTransform;
    parentId?: string;
    subType?: string;
    children?: CompositionGraphicsNode[];
}
export declare function getCompositionGraphics(comp: any): CompositionGraphics;
export {};
